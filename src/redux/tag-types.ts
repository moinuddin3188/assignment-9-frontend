export enum tagTypes {
    user = "user",
    admin = "admin",
    service = "service",
    category = "category",
    blog = "blog",
    faqs = "faqs",
    review = "review",
    booking = "booking",
  }
  
  export const tagTypesList = [
    tagTypes.user,
    tagTypes.admin,
    tagTypes.category,
    tagTypes.service,
    tagTypes.blog,
    tagTypes.faqs,
    tagTypes.review,
    tagTypes.booking
  ];